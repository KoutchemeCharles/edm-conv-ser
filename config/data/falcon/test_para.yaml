name: "falconcode_test-para"
split: "test"

# Preprocessing
preprocessing:
  remove_exams: true 
  remove_non_compiling: true 
  remove_runtime_errors: false 

# Individual student processing
student:
  min_grade: 50
  format: "code"
  feedback_level: null 
